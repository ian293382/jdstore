<div class="col-md-8 col-md-offset-2 admin-data-box">
  <hr style="size:2px" />
  <% if @category_group.image.present? %>
    <%= image_tag(@category_group.image.url, :class => "width-100") %>
  <% end %>
  <%= simple_form_for [:admin, @category_group] do |f| %>
    <%= f.input :name, :label => t('category-group-name') %>
    <%= f.input :image, :label => t('image-upload') %>
    <span class="hidden-box"><%= f.input :is_hidden, :label => t('hide') %></span>
    <div class="form-actions">
      <%= f.submit t('submit'), class: 'btn btn-md btn-default' %>
    </div>
  <% end %>
</div>
